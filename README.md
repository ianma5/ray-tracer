# Ray Tracer Setup

- The libraries OpenGL, glfw3, and GLEW are required
- This project was programmed on Linux so libraries may differ
- Ray tracer assignment with shading, shape intersection, perspective/orthographic camera

To compile the program run:

`make`

`./raytracer`

